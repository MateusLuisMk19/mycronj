instalar pacote no windows:
py -m pip install nome_do_pacote

criar ambiente virtual:
py -m venv venv

ativar ambiente virtual:
venv\Scripts\activate

desativar ambiente virtual:
deactivate

rodar o projeto:
python main.py

py -m pip install auto-py-to-exe

pip show auto-py-to-exe

auto-py-to-exe